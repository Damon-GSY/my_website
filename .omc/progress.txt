Editorial portfolio redesign progress

Branch: codex/editorial-portfolio-clean
Pre-redesign baseline: npm run build and npm run lint passed before dependency or asset changes.

Task 1 post-install verification (2026-07-10):
- npm install p5 animejs completed without peer dependency errors.
- A fresh npm run build passed after dependency installation.
- A fresh npm run lint passed after dependency installation.
- The checked-in source PNG is 2944x1648, 5,394,451 bytes, and retains SHA-256 fe8f717fa83332050cdbb98d123caf087ecb7857b86e301f041d4aa37abbe711.
- The desktop derivative is 2200x1232, 52,546 bytes, and retains SHA-256 b796829c5d92ceea0e6cf5746d4735efaa6f221c13c5b3b716a7ecd5a3b31e8e.
- The mobile derivative is 1100x616, 13,736 bytes, and retains SHA-256 91514bb81f51a083d741fd5f8b0451d331b1ff469f845bd49014d56b8c41c214.
- Both WebP derivatives are within their PRD byte budgets and visually preserve the source composition.
- The image assets are not imported by application code yet, so Vite emission is intentionally deferred to US-001.

Source provenance: gdamon0208_A_scientific_visualization_of_an_optimization_land_83636679-68fb-428b-a208-a08ad142daab_1.png, identified by the SHA-256 above.
Local-only import location: the source was supplied from the user's Downloads directory; that absolute path is not a portable project reference.
Next story: US-001 — Build the identity-first optimization landscape hero.

US-001 implementation notes (2026-07-10):
- Replaced the dashboard-style Hero with an identity-first editorial first viewport containing Damon Guo-Shiyu, LLM Engineer at Alibaba, Hangzhou, the four required research focus areas, and direct links to #work and #contact.
- Added OptimizationLandscape with a responsive picture using only optimization-landscape-2200.webp and optimization-landscape-1100.webp; the provenance PNG is not imported by runtime code.
- Added scoped GSAP/ScrollTrigger terrain depth and copper beacon motion, plus a mutable scroll-progress ref consumed by a dynamically imported p5 ridge-particle overlay.
- Motion cleanup covers the p5 instance, ResizeObserver, GSAP context, owned ScrollTriggers, and the motion-preference listener. Reduced-motion mode skips GSAP, p5, and anime.js entrance motion while leaving the complete image and content visible.
- anime.js v4 createScope/animate provides the short hero entrance; the scope is created after declaration and reverted on unmount.
- Fresh npm run lint passed.
- Fresh npm run build passed and emitted the 13.74 kB mobile WebP and 52.55 kB desktop WebP. The p5 implementation remains in a separately requested runtime chunk rather than the initial entry.
- git diff --check passed. The unrelated untracked tools/_diag_lab.cjs and tools/_verify_lab.cjs files remain untouched.
- In-app Browser discovery returned no available browser instance, so desktop/mobile/reduced-motion screenshots, live DOM overflow checks, console checks, and Codex visual acceptance remain pending for the main agent. US-001 passes remains false until that evidence is recorded.

US-001 specification follow-up (2026-07-10):
- Narrowed the scoped anime.js entrance to four explicit targets only: the eyebrow, the two independently staggered name-line spans inside one semantic h1, the main description, and the action row.
- Decorative index copy, the role line, focus list, factual proof, and landscape caption are no longer anime.js targets and remain immediately visible.
- Added a separate compact repository-backed proof line: 4 research works; NUS Statistics, Top 5%; and UNSW Computer Science, Top 3%. These facts are grounded in src/data/about.js and are presented as editorial evidence rather than dashboard metrics.
- US-001 remains false until the pending browser screenshots and Codex visual acceptance are completed.

US-001 performance follow-up (2026-07-10):
- Deferred the p5 dynamic import to requestIdleCallback with a 700 ms timeout and a 160 ms setTimeout fallback for browsers without the idle API; pending scheduling is cancelled during cleanup and the responsive image remains immediate.
- Added a p5-owned IntersectionObserver on the landscape scene. The sketch starts paused, resumes only while the scene intersects, and returns to noLoop when offscreen; observer callbacks and setup guard against disposal races.
- Cleanup now disconnects both p5 observers, removes the p5 instance, cancels pending idle/timer work, and reverts the GSAP context. Redundant manual ScrollTrigger killing was removed because the triggers are owned by that context.
- US-001 remains false pending live browser and Codex visual acceptance.

US-001 live visual evidence (2026-07-10):
- Desktop 1440x1000 screenshot `/private/tmp/my_website-hero-desktop-viewport.png` shows the full identity, role, four focus areas, profile proof, and both actions above the fold with the optimization landscape behind them. DOM measurements report no horizontal overflow and one decorative p5 canvas.
- At scrollY 560, `/private/tmp/my_website-hero-desktop-midscroll.png` records the terrain plane using a non-identity `matrix3d` transform and the copper beacon translated, scaled, and faded along the scroll path.
- Mobile 390x844 screenshot `/private/tmp/my_website-hero-mobile-top.png` shows the complete identity, role, focus, proof, and actions without clipping or horizontal overflow. The browser selected `optimization-landscape-1100.webp` as the current image source.
- Reduced-motion screenshot `/private/tmp/my_website-hero-reduced-motion.png` verifies `prefers-reduced-motion: reduce`, scene state `true`, zero canvases, no plane/beacon transforms, no horizontal overflow, both actions above the fold, and zero captured console errors.
- A later normal-mode in-app console-log read was denied by the browser security policy, so normal-mode console cleanliness remains explicitly unverified rather than inferred.
- Codex screenshot review could not run because the account usage limit was exhausted. The first artifact is `.omc/artifacts/ask/codex-read-these-images-by-absolute-path-private-tmp-my-website-he-2026-07-10T03-42-48-470Z.md`; the required escalated retry was rejected with a retry time of 15:55 on 2026-07-10.
- Damon explicitly authorized implementation to continue while this external Codex gate is deferred. US-001 remains false and must receive the required Codex screenshot PASS before final Ralph completion.
- Next implementation story: US-002 — Present selected work as editorial case studies.

US-002 implementation notes (2026-07-10):
- Replaced the homepage work carousel with an information-first editorial ordered list using exactly supply-chain-agent-system, dynamic-tool-resolution-agents, and multi-turn-agent-evaluation from src/data/projects.js.
- Each case exposes its real title, kicker, category, year, stage, description, first details contribution, and outcome without requiring hover, expansion, or carousel interaction. A visible keyboard-focusable link routes to /projects.
- Added a restrained IntersectionObserver-driven case marker on desktop. It follows section reading position rather than the cursor, does not alter content visibility, and is omitted on mobile and under prefers-reduced-motion: reduce.
- Simplified HomePage composition to Hero, Work, Journal, and Contact. Marquee, Capabilities, SystemShowcase, Agent OS chapter state, navigation rail, and their homepage wrappers no longer render; all routes and global components remain intact.
- Fresh npm run lint passed, fresh npm run build passed, and git diff --check passed.
- A local Playwright smoke check at 1440x1000 and 390x844 found exactly three requested cases and their outcomes. document.scrollWidth equaled document.clientWidth at both sizes (1440/1440 and 390/390); the case grid resolved to three columns on desktop and one DOM-order column on mobile.
- Desktop marker checks confirmed the active state advances from the supply-chain case to dynamic tool resolution and then multi-turn evaluation as each row reaches the reading position.
- Local screenshots `/private/tmp/selected-work-desktop.png` and `/private/tmp/selected-work-mobile.png` were inspected for implementation self-review only. Main-agent live visual evidence and the required Codex screenshot review remain pending, so US-002 passes remains false.

US-002 quality-review follow-up (2026-07-10):
- Removed the module-evaluation throw for a missing configured project ID. Available configured projects now render in order, a DEV-only warning names every missing ID, and a neutral archive-directed message is shown if none remain; no project claims are invented.
- Reworked the case IntersectionObserver lifecycle to react to desktop-width and reduced-motion media-query changes. The observer disconnects when either desktop eligibility is lost or reduced motion is enabled, and reinstalls when eligibility returns.
- Intersection ratios are retained for every observed row across callback batches before the active case is selected, preventing a partial callback batch from incorrectly overriding a more visible row. Content rendering remains fully independent of observer state.
- Fresh npm run lint passed, fresh npm run build passed, and git diff --check passed.
- A live resize/motion Playwright smoke check kept all three cases visible throughout: desktop active tracking worked; at 390x844 the marker was hidden, observer-driven state stopped changing, and scrollWidth/clientWidth stayed 390/390; restoring 1440x1000 restored active tracking; enabling reduced motion hid and paused the marker; restoring motion reinstated it. Desktop scrollWidth/clientWidth stayed 1440/1440.
- A temporary missing-ID fault injection rendered the two remaining configured real projects, kept the Selected Work heading and page mounted, emitted exactly `[Work] Missing selected project IDs: missing-project-smoke-test`, and produced zero page or console errors. The real supply-chain ID was restored immediately after the check.
- US-002 passes remains false pending main-agent live visual and required Codex screenshot acceptance.

US-003 implementation started (2026-07-10):
- Damon explicitly authorized continued editing and committing after the previously reported usage-limit gate.
- Added the Research & experience narrative between Selected work and Journal. It derives four chronology nodes from `timelineData`, the section framing from `aboutProfile`, and exactly four research-index entries from `projects.filter(category === 'research')`.
- Current Alibaba work is the strongest node; MSRA, NUS, and UNSW progressively quiet along one continuous scientific-editorial path. The entries visibly retain Hangzhou, M365 GPT-4o, the NUS Statistics Top 5% / QS #8 evidence, and the UNSW Computer Science Top 3% / QS #19 evidence.
- Only the first chronology entry receives a short Framer Motion stagger. Reduced-motion mode renders it immediately with opacity 1 and transform none; there is no scrubbed or continuous section motion.
- Fresh `npm run lint`, `npm run build`, and `git diff --check` passed.
- Live browser checks found 4 chronology items, 4 research works, a `/about` link, and every required data-backed claim. At 1440x1000 and 390x844, `document.scrollWidth` equaled `document.clientWidth`; the 390px article grid resolved to one column. Browser page errors were empty.
- Self-reviewed screenshots: `/private/tmp/research-path-desktop.png`, `/private/tmp/research-path-mobile.png`, `/private/tmp/research-path-reduced.png`, and `/private/tmp/research-path-index-desktop.png`.
- US-003 remains false pending the deferred Codex screenshot acceptance gate.

US-004 implementation and verification (2026-07-11):
- Damon explicitly authorized continued editing after the previously disclosed write gate. All work was performed only in the clean `/private/tmp/my_website-editorial-clean` worktree on `codex/editorial-portfolio-clean`.
- Replaced the homepage event-log treatment with a dark scientific-editorial Writing section. The latest four real posts expose title, date, category, complete excerpt, and reading time; every entry links to `/blog/:slug`, and the section links to `/blog`.
- Replaced the Aurora/Magnetic contact card with a direct editorial conclusion, four collaboration areas, visible `hello@damon.ai`, and text-forward channel links.
- Omitted the mismatched LinkedIn profile. The initially retained GitHub, YouTube, and Bilibili profile URLs were subsequently removed during specification review because the available evidence did not directly and authoritatively verify their ownership; no replacements were invented.
- Rebuilt the navbar as a restrained fixed editorial rule with Damon Guo-Shiyu's identity, route-aware text links, theme control, and a conversation link. Removed LiveTime/CST, status pulses, circular D status mark, dashboard capsule, and animated nav pill.
- Preserved the skip link, mobile body scroll lock, route-close behavior, Escape dismissal, and ARIA state. Escape now restores focus to the menu trigger; blog-detail routes correctly retain Writing as active.
- Replaced the map/presence footer with a quiet closing signature and corrected the wrong `Shengyue Guan` attribution to Damon Guo-Shiyu. Removed FootprintMap, pings, fake status, and dashboard language.
- Removed FloatingWindow from App while preserving routes, ScrollProgress, Suspense, and page transitions.
- Fresh `git diff --check`, `npm run lint`, and `npm run build` passed. The build retains only the pre-existing large-chunk advisory.
- Live browser checks found exactly four Writing entries with all required metadata, the exact confirmed email URL, and no LinkedIn, live clock, pulse/ping classes, or FloatingWindow rendering. Browser page errors were empty.
- At 1440x1000 and 390x844, `document.scrollWidth` equaled `document.clientWidth` (1440/1440 and 390/390). At 390px, the menu locked body scroll and focused Close; Escape removed the dialog, cleared the lock, and restored focus to Open menu.
- Active-route checks returned Writing for `/blog` and `/blog/why-agent-evaluation-is-hard`, and Work for `/projects`.
- Reduced-motion emulation reported zero running animations, all four Writing rows at opacity 1 with transform none, and no horizontal overflow.
- Self-reviewed screenshots: `/private/tmp/editorial-home-desktop.png`, `/private/tmp/editorial-home-mobile.png`, `/private/tmp/editorial-home-mobile-menu.png`, `/private/tmp/editorial-home-reduced.png`, `/private/tmp/editorial-writing-contact-desktop.png`, `/private/tmp/editorial-writing-mobile.png`, and `/private/tmp/editorial-contact-light.png`.
- US-004 remains false pending the deferred Codex screenshot acceptance gate.

US-003 quality-review follow-up (2026-07-11):
- Replaced substring title matching and point-array indices with four explicit timeline-node configs. Every config now has a stable logical ID plus exact year, track, and title fields; only one exact match may render, so similar titles are never silently selected.
- Missing and duplicate configured nodes are omitted without crashing. DEV warnings precisely identify the logical ID and exact match tuple; duplicate warnings also report the match count.
- Visible evidence is selected by meaningful point keywords rather than positions: supply-chain domain LLM / dynamic tool registration, GPT-4o / memory architecture, Statistics / QS Global Rank #8 / Top 5%, and Computer Science / QS Global Rank #19 / Top 3%. If matching evidence is unavailable, the component falls back only to truthful location/profile copy.
- Removed research-index slicing and static topic pairing. The count and rows derive from the complete current `projects.filter(category === 'research')`; all four current works render title, kicker, year, and stage, distinguishing the three papers from the MSRA internship research entry.
- Raised essential section index, path axis, year/annotation, track, location, research kicker, and year/stage metadata to 11.2px. Their composited contrast is 7.20:1 on `#0b0c0c`; the quietest UNSW title is 8.46:1. NUS and UNSW remain visibly legible while hierarchy is carried by spacing, scale, rules, and weight.
- Fresh `npm run lint`, `npm run build`, and `git diff --check` passed after the fix. The production build retains only the pre-existing large-chunk advisory.
- Playwright checks at 1440x1000, 390x844, and reduced motion found 4 chronology nodes and 4 research works, no page errors, no horizontal overflow (1440/1440 and 390/390), one-column mobile chronology, and all five required evidence terms visible. Reduced motion renders the first entry at opacity 1 with no transform.
- A temporary DEV fault injection produced the exact missing and duplicate warnings, omitted both invalid nodes, kept the section mounted with the other two real chronology nodes and all four research works, and produced zero page/console errors. The fault injection was removed immediately after verification.
- Self-reviewed screenshots: `/private/tmp/research-path-quality-desktop.png`, `/private/tmp/research-path-quality-mobile.png`, `/private/tmp/research-path-quality-reduced.png`, and lower chronology/index capture `/private/tmp/research-path-quality-lower-desktop.png`.
- US-003 remains false pending the deferred Codex screenshot acceptance gate.

US-004 specification-review follow-up (2026-07-11):
- PageTransition now reads `prefers-reduced-motion` through Framer Motion. Normal navigation retains the 300ms opacity transition; reduced-motion navigation has no initial or exit fade and uses a zero-duration transition.
- Completed mobile modal behavior with a bidirectional Tab/Shift+Tab focus trap, initial Close-button focus, and focus restoration to the menu trigger after Escape, overlay click, navigation-link selection, the conversation link, and explicit Close.
- While the dialog is open, main and footer are inert and body scrolling is locked. Cleanup restores the exact prior body overflow and inert property/attribute state on close, route change, or unmount.
- Removed the unverified GitHub, YouTube, and Bilibili profiles from Contact. The mismatched LinkedIn URL remains omitted. Only the confirmed `mailto:hello@damon.ai` contact and internal site links are displayed; external personal profiles remain pending direct confirmation.
- Acceptance remains: only verified profiles are displayed. The current verified external social-profile count is zero.
- Fresh `git diff --check`, `npm run lint`, and `npm run build` passed; the build retains only the pre-existing large-chunk advisory.
- Normal-motion frame sampling during home-to-blog navigation observed opacity from 1 through intermediate values to 0 across the retained transition. Under reduced motion, every sampled frame stayed at opacity 1, the `/blog` route became current immediately, and zero page-transition animations were present.
- Mobile checks at 390x844 confirmed initial Close focus, Shift+Tab wrapping from Close to the final conversation link, and Tab wrapping back to Close. Main/footer were inert and body overflow was hidden only while open; Escape restored their exact prior false/no-attribute/empty states and returned focus to Open menu.
- Overlay click and a Work navigation-link click each closed the dialog, restored Open-menu focus, removed inert/body lock, and the link completed navigation to `/projects`.
- Identity-safety checks found zero external HTTP profile links, no LinkedIn/GitHub/YouTube/Bilibili text, exactly the confirmed `mailto:hello@damon.ai` channel, expected internal links, no horizontal overflow (390/390), and no browser page errors.
- Revised contact screenshot: `/private/tmp/editorial-contact-verified-mobile.png`.
- US-004 remains false only for the deferred Codex screenshot acceptance gate.
