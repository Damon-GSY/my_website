# Codex Optimization Landscape Implementation Plan

> **For Claude:** REQUIRED SUB-SKILL: Use frontend-design to implement and verify this plan task-by-task.

**Goal:** Build Damon's cinematic, scroll-driven portfolio in one continuous mathematical optimization space.

**Architecture:** A 520svh document drives one sticky 100svh viewport. `src/App.tsx` owns the scroll engine, four independent image layers, particles, atmosphere, two scene transitions, personal content, and reduced-motion behavior. The four user-provided images remain separate runtime layers.

**Tech Stack:** React 19, TypeScript 5, Tailwind CSS 4, Vite 6, requestAnimationFrame, CSS transforms.

---

1. Compress four source PNGs to delivery WebP assets under `public/assets`.
2. Replace the routed JSX application with a focused TypeScript single viewport.
3. Drive landscape, depth, light, and foreground layers from one scroll progress value.
4. Preserve verified role, education, location, research focus, and contact links from `main`.
5. Run TypeScript, ESLint, and Vite production build.
6. Start `127.0.0.1:4173` for user review and commit only this branch.
