import { profile } from '@/lib/content'

export const socialImage = {
  url: '/opengraph-image',
  width: 1200,
  height: 630,
  alt: `${profile.name} — ${profile.role} building agent systems under control`,
}
